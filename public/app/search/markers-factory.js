angular.module('search.roommi')

.factory('markersFactory', function() {

	// getListings = function(geopoint) {

	// 	var Listing = Parse.Object.extend('listing');
	// 	var locquery = new Parse.Query(Listing);
	// 	locquery.withinMiles('geopoint', geopoint, 100);
	// 	locquery.find({
	// 		success: function(listings) {
	// 			console.log(listings);
	// 			return listings;
	// 		},
	// 		error: function(error) {
	// 			console.log('error finding listings by this location');
	// 		}
	// 	})



	// 	// var Listing = Parse.Object.extend("listing");
	// 	// var Marker = Parse.Object.extend("marker");
	// 	// var innerQuery = new Parse.Query(listing);
	// 	// innerQuery.lessthan('rent', price+1);
	// 	// var query = new Parse.Query(Marker);
	// 	// query.matchesQuery("listing", innerQuery);
	// 	// query.find({
	// 	//   success: function(markers) {
	// 	//     // comments now contains the comments for posts with images.
	// 	//   }
	// 	// });


	// }

	
})

